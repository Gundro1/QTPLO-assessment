{
  "name": "qa-assessment",
  "version": "1.0.0",
  "description": "QA Assessment - Automation & API Tests using Playwright",
  "scripts": {
    "test": "npx playwright test",
    "test:login": "npx playwright test tests/login.spec.js",
    "test:cart": "npx playwright test tests/cart-and-checkout.spec.js",
    "test:api": "npx playwright test tests/api.spec.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
